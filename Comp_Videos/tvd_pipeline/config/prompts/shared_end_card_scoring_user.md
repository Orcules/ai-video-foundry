I have {num_candidates} surprise animation clips from a {business_category} business advertisement. I need to pick the BEST one to use as a ~2 second end card (outro) with text overlay (business name, address, phone).

Here are the candidates:

{clip_descriptions}

Return a JSON object with:
- "best_index": the 0-based index of the best clip for the end card
- "reason": a brief explanation (1 sentence) of why this clip is the best choice
